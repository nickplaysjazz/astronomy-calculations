[project]
name = "astronomy-calculations"
version = "1.0.0"
description = "Calculations written in Python to obtain astronomical information for a given date."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "black>=26.5.1",
    "numpy>=2.4.6",
    "prettytable>=3.17.0",
]
